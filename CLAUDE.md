Ediro：チャットに貼る前の下書きに使う単一ウィンドウの Markdown エディタ。macOS 専用、個人利用。
App Store 配布はせず ad-hoc 署名で自分用にビルドする。

## 変更の検証

このリポジトリはコーディングエージェントが自力で検証を完結できることを設計上の要件にしている。
実装を変えたら結果を確かめる。目視の印象だけで完了にしない。

`.xcodeproj` を持たない構成のため XCUITest は使えない。何を変えたかで経路が決まる。

- **ロジック、および純粋な SwiftUI ビューの見た目** — `swift test`。オフスクリーン描画して画素を
  検査できる。画面収録権限もアプリ起動も要らず決定論的なので、回帰はここで押さえる
- **エディタを含む合成** — `swift test` では描けない (理由は `.claude/rules/editor-ui.md`)。
  属性・フォント解決・ハイライトまで分解して `swift test` に落とし、残りを `make run && make shot`
  で一度だけ確認する。撮影は非決定的なので回帰の防波堤にはしない
- **メニュー操作やキー入力** — `osascript` + System Events。アクセシビリティ権限が要る。
  権限が無ければ他の経路で代替せず、付与の手順を提示して止まる。App メニューの `Preferences…` は
  macOS が `Settings…` に自動改称するため、その名前で参照する

## 開発用ビルドと日常使いのビルド

`make` の既定は開発用ビルド、日常使いは `make release`。両者は名前・bundle identifier・アイコンの色が
違うため、設定と下書きの保存先も分かれる。

アプリを名前で指すときは、どちらの名前かに注意する。`pgrep` や `pkill` は実行ファイル名 (`EdiroDev`)、
`osascript` の `application process` とウィンドウの撮影はバンドル名 (`Ediro Dev`) で照合する。
